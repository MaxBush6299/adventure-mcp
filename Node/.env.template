# Environment Variables Template for RLS Implementation
# Copy this to .env in the Node directory and fill in your values

# ==========================================
# Azure AD / Entra ID Configuration
# ==========================================
# These values come from your App Registration (Task 1.1)
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here

# ==========================================
# Azure SQL Database Configuration
# ==========================================
SERVER_NAME=adventureworks8700.database.windows.net
DATABASE_NAME=adventureworks

# ==========================================
# MCP Server Configuration
# ==========================================
NODE_ENV=development
MCP_TRANSPORT=http
PORT=8080
READONLY=false

# Authentication settings (Task 1.2)
# Set REQUIRE_AUTH=true to enforce JWT token validation on all requests
# If false, tokens are validated if present but not required
REQUIRE_AUTH=false

# ==========================================
# Connection Settings
# ==========================================
CONNECTION_TIMEOUT=30
TRUST_SERVER_CERTIFICATE=false

# ==========================================
# Optional: Azure Key Vault (for production)
# ==========================================
# If using Key Vault, the app will fetch secrets from there instead
# KEY_VAULT_NAME=your-keyvault-name
